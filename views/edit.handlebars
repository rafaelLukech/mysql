<h2>Editar Livro</h2>

<form action="/edite/save" method="POST">
    <input type=""hidden" value="{{book.id}}" name="id">
    <div>
        <label for="">Título:</label>
        <input type="text" name="title" placeholder="Digiteo título" id="title" value="{{book.tilte}}">
    </div>
    <div>
        <label for="">Número de paginas:</label>
        <input type="number"="pageqty" placeholder="Quantas páginas o livro possui?" id="pageqty value="{{bok.pageqty}}>
    </div>

    <button type="submit">cadastrar</button>
</form>